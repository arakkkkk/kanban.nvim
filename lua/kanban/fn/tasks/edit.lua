local M = {}

function M.add_due(kanban, task)
  local markdown = kanban.ops.markdown
end

function M.add_tag(kanban, task)
  local markdown = kanban.ops.markdown
end

function M.restrict_input(kanban, task)
  local markdown = kanban.ops.markdown
end
return M
